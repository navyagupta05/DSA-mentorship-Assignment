#include <stdio.h>

int main()
{
    int n, a=0;
    printf("enter a number ");
    scanf("%d", &n);
    while (n!=0)
    {
        n=n/10;
        a++;
    }
    printf("the number of digits in the number is %d", a);
    
    return 0;
}
